using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    public float moveSpeed;

    [SerializeField]
    private MonsterPath thePath;              // ???????? ?????? ???? path????
    private int currentPoint;               // ???? ?????? point?? ?????? ?????? ???????? ????
    private bool reacheEnd;                 // ???? ???? ????
    // Start is called before the first frame update
    void Start()
    {
        if (thePath == null)
        {
            thePath = FindObjectOfType<MonsterPath>();        // ???? ?????????? ???????? EnemyPath?? ???? ?????????? ????????
        }
    }

    // Update is called once per frame
    void Update()
    {
        if (reacheEnd == false)              // if(!reacheEnd) ???? ????
        {
            transform.LookAt(thePath.points[currentPoint]);     // ???????? ???? ?????? ?????? ????.(LookAt????)

            // MoveTowards???? (??????, ????????, ??????)
            transform.position =
                Vector3.MoveTowards(transform.position, thePath.points[currentPoint].position, moveSpeed * Time.deltaTime);

            // Vector3.Distance (A,B) ?????? ???? => ?????? 0.01???? ?? ???? ?????????? ????
            if (Vector3.Distance(transform.position, thePath.points[currentPoint].position) < 0.01f)
            {
                currentPoint += 1;              // ???? ???????? ????

                if (currentPoint >= thePath.points.Length)       // ?????? ???? ?? ???? ???? ???????? ???? ????
                {
                    reacheEnd = true;
                }
            }

        }
    }
}